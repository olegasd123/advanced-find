
export const ResultGrid = () => {
  return (
    <>
      <div>
        <div className="w-36 grow-2">
          // TODO: `Back` button to return to filter page
        </div>
        <div className="w-36 grow-3">
        </div>
        <div className="w-24 grow-2"></div>
        <div className="w-64 grow-8">
        </div>
      </div>

      // TODO: create a table and display search results in it.
      // the table's' columns should created dynamically from `ResultView.TableColumns` section of the entity configuration and the data from the search results.
      // Create a skeleton loader for the table while the search results are being loaded
    </>
  )
}